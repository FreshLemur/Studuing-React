React - бібліотека для створення UI.
    Ми можемо використовувати його як для одної сторінки, так і для окремих її компонентів.
    Заснований на компонентах, тому він складається з компонентів.
    Був створений FaceBook.

React базується на компонентах і складається з багатьох маленьких компонентів.

Ми пишемо на JSX.
    А це JavaScript + XML, дозволяє поєднувати HTML та JS.